# Базовый образ vLLM (NVIDIA CUDA). Сборка: docker-build.bat
# При необходимости добавьте сюда установку доп. зависимостей, например:
#   RUN uv pip install --system vllm[audio]==0.11.0
FROM vllm/vllm-openai:latest
# Метаданные для кастомного образа (опционально)
LABEL maintainer="vllm-usage-examples"
